## Section06

### /volatile - 메모리 가시성
* 예상하는 프로그램 동작 방식
  ![img.png](image/image01.png)
* 실제 프로그램이 동작 방식
  ![img.png](image/image02.png)
  * 컴퓨터에는 캐시 메모리라는 것이 존재한다.
  * 빠른 연산을 위해 프로그램은 메인 메모리에 있는 데이터를 캐시 메모리로 불러들인다.
  * 스레드에서 아무리 값을 바꿔도 메인 메모리의 값이 바뀌는게 아니라 캐시 메모리의 데이터가 변하는 것이다.
    * 그리고 캐시 메모리의 값은 메인 메모리에 즉시 반영되지 않는다.
* 캐시 메모리를 메인 메모리에 반영하거나, 메인 메모리의 변경 내역을 캐시 메모리에 다시 불러오는 정확한 시점을 알 수는 없다.
  * 다만 주로 컨텍스트 스위칭이 될 때, 캐시 메모리도 함께 갱신되지만 이 역시도 환경에 따라 다를 수 있다.
* volatile
  *  성능을 약간 포기하는 대신에, 값을 읽을 때, 값을 쓸 때 모두 메인 메모리에 직접 접근하도록 만든다.

### 자바 메모리 모델
#### 메모리 가시성
* 한 스레드가 변경한 값이 다른 스레드에서 언제 보이는지에 대한 것

#### 자바 메모리 모델
* 여러 스레드들의 작업 순서를 보장하는 happens-before 관계에 대한 정의
* happens-before
  * 한 동작이 다른 동작보다 먼저 발생함을 보장함
  * 스레드 간의 메모리 가시성을 보장하는 규칙
  * happens-before가 성립하면, 한 스레드의 작업을 다른 스레드에서 볼 수 있게 된다. (참조시 최신 상태 보장)

#### happens-before 관계 발생 경우
* 프로그램 순서 규칙
  * 단일 스레드에서 코드가 입력된 순서대로 실행됨이 보장됨
* volatile 변수 규칙
  * 한 스레드에서 volatile 변수에 대한 쓰기 작업은 해당 변수를 읽는 모든 스레드에 보여지는 것이 보장됨
* 스레드 시작 규칙
  * start() 호출 전에 수행된 모든 작업은 새로운 스레드가 시작된 후의 작업보다 먼저 실행됨이 보장됨
* 스레드 종류 규칙
  * join 대상 스레드의 모든 작업은 join() 이 반환된 후의 작업보다 먼저 실행됨이 보장됨
* 인터럽트 규칙
  * Thread.interrupt() 를 호출하는 작업이, 인터럽트된 스레드가 인터럽트를 감지하는 시점의 작업 보다 happens-before 관계
* 객체 생성 규칙
  * 객체의 생성자는 객체가 완전히 생성된 후에만 다른 스레드에 의해 참조될 수 있도록 보장됨
* 모니터 락 규칙
  * 한 스레드에서 synchronized 블록을 종료한 후, 그 모니터 락을 얻는 모든 스레드는 해당 블록 내의 모든 작업을 볼 수 있음
* 전이 규칙
  * A가 B보다 happens-before 관계에 있고, B가 C보다 happens-before 관계에 있다면, A는 C보다 happens-before 관계

#### 정리
* volatile 또는 스레드 동기화 기법을 사용하면 메모리 가시성의 문제가 발생하지 않는다.